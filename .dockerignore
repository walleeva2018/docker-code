# Git repository - Not needed in Docker image, reduces build context size significantly
.git
.gitignore

# Environment file - Already injected via env_file in docker-compose.yml, shouldn't be baked into image
.env

# Secrets folder - Already mounted via Docker secrets, must not be copied into image for security
secrets/

# Docker files - Not needed inside the container, reduces image size
docker-compose.yml
Dockerfile
.dockerignore
